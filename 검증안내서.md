# T04 검증안내서

기준 문서: `spec/public-contract.json`, `spec/criterion-registry.json` (조건 35개, 전수 판정)

## 무엇을 만들었나

비개인 공개 원천(Open-Meteo)에서 실제로 변하는 기온 값을 조회해 값·단위·출처·출처 시각·조회 시각·기준 시간대(Asia/Seoul)를 화면에 보여주고, `normalized-reading.schema.json` 형식으로 저장하는 날씨 정보판입니다. 실패해도 마지막 정상값을 지우지 않고, 실패 원인을 `timeout / auth / rate_limit / offline / schema_error` 5가지로 구분해서 보여줍니다.

증거는 두 갈래로 나뉩니다.

- **실제 라이브 증거** (`현재 날씨`, `기록·비교` 탭): 진짜 API를 호출한 결과. C03~C10, C22~C24가 여기서 확인됩니다.
- **합성 재생 증거** (`합성 재생` 탭): `spec/fixture-manifest.json`에 등록된 9개 결정론 fixture를 `t04-adapter.js`에 그대로 흘려보낸 결과. C12~C21, C26이 여기서 확인됩니다. 실제 API를 호출하지 않고, 저장소도 `alephT04ReplayStateV1`로 완전히 분리되어 있어 실제 기록(`alephT04StateV1`)에 영향을 주지 않습니다.

## 제출물

- 결과물 URL: `https://gamedh823-cmd.github.io/T04/`
- 소스 저장소 URL: 'https://github.com/gamedh823-cmd/T04'

## 재현·통과 확인 — 실제 라이브 조회 (C03~C10)

1. 어디로 가나요: 공개 결과물 URL 접속 → 상단 "현재 날씨" 탭
2. 무엇을 하나요(3단계 이내):
   1) 시·도 / 시·군·구 / 읍·면·동 선택
   2) "선택 지역 날씨 보기" 클릭
   3) "🧾 조회 정보" 카드 확인
3. 무엇이 보이면 통과:
   - 값(`reading-value`) · 단위(`reading-unit`) · 출처(`reading-source`, Open-Meteo) · 출처 시각(`reading-source-time`) · 조회 시각(`reading-fetched-at`) · 기준 시간대(`reading-timezone`, Asia/Seoul)가 모두 표시됨
   - 상태 배지가 "정상 · fresh/none"
   - "출처 원자료 주소" 링크를 열면 실제 호출한 Open-Meteo API 응답을 그대로 볼 수 있음
4. 안 될 때 무엇이 보이나요:
   - 상태 배지가 "실패 · stale/{timeout|auth|rate_limit|offline|schema_error}" 중 하나로 표시
   - 값 칸에는 방금 실패한 값이 아니라 **마지막으로 성공했을 때의 값**이 그대로 남아 있음
   - "↻ 다시 시도" 버튼 노출

## 재현·통과 확인 — 서로 다른 실제 날짜 2건 (C22~C24)

1. 어디로 가나요: "기록·비교" 탭
2. 무엇을 하나요: 서로 다른 Asia/Seoul 날짜에 위 라이브 조회를 한 번씩 수행(같은 지역 기준)
3. 무엇이 보이면 통과:
   - 표에 날짜가 다른 행이 2건 이상 쌓임
   - 각 행의 값·단위·출처 시각이 그 날 실제로 조회했던 값과 일치
   - "최근 두 기록 비교" 카드에 두 값의 차이가 날짜순으로 재계산되어 표시됨
4. 안 될 때: 기록이 1건뿐이면 비교 카드에 "2건 이상 쌓이면 표시됩니다" 문구가 남아 있음

## 재현·통과 확인 — 합성 재생 (C12~C21, C26)

1. 어디로 가나요: "합성 재생" 탭
2. 무엇을 하나요(3단계 이내):
   1) 시나리오 드롭다운에서 확인하고 싶은 항목 선택 (정상 저장 순서 / 5가지 실패 각각 / 오류 뒤 회복)
   2) "재생" 클릭
   3) 결과 카드 확인
3. 무엇이 보이면 통과: `freshness`, `error_code`, `row_count`, `stored_value`, `delta` 5개 항목 모두 ✅ (해당 fixture의 `expected` 값과 일치)
4. 안 될 때 무엇이 보이나요: 해당 필드 옆에 ❌ 표시와 실제값/기대값이 함께 나타남 — 이 상태로는 제출하면 안 됨

## AI와 내 판단

1. AI에게 맡긴 일: `normalized-reading`/`reading-status` 스키마 반영, 실패 상태 5종 분기, `t04-adapter.js`(공식 fixture 어댑터의 브라우저 포팅), 합성 재생 탭 UI·로직, GitHub Pages 배포
2. 내가 직접 판단한 일: (직접 채워주세요 — 예: 어떤 지역/신호를 추적할지, 재생 결과가 실제로 9개 fixture와 다 일치하는지 하나씩 눌러본 것, 실제 두 날짜 기록이 맞는지 화면과 API 응답을 대조해본 것 등)
3. AI 말을 안 들은 일 (없으면 이유): (직접 채워주세요)
