# T04 검증안내서 · 체크리스트

기준: `spec/public-contract.json`, `spec/criterion-registry.json` (총 35개 조건, 전수 판정)

## 제출물

- 결과물 URL (필수): `https://` (GitHub Pages 배포 후 채우기)
- 소스 저장소 URL (필수): `https://github.com/.../commit/<40자리 또는 64자리 commit 해시>`

## 재현·통과 확인 4가지

1. 어디로 가나요: 공개 결과물 URL 접속 → 상단 "현재 날씨" 탭
2. 무엇을 하나요(3단계 이내):
   1) 시·도/시·군·구/읍·면·동 선택
   2) "선택 지역 날씨 보기" 클릭
   3) "조회 정보" 카드 확인
3. 무엇이 보이면 통과:
   - 값·단위·출처(Open-Meteo)·출처 시각·조회 시각·기준 시간대(Asia/Seoul)가 모두 표시됨
   - 상태 배지가 "정상 · fresh/none"
4. 안 될 때 무엇이 보이나요:
   - 상태 배지가 "실패 · stale/{timeout|auth|rate_limit|offline|schema_error}" 중 하나로 표시
   - 마지막 정상값은 지워지지 않고 그대로 남아 있음
   - "↻ 다시 시도" 버튼 노출

## AI와 내 판단 3줄

1. AI에게 맡긴 일: (예: normalized-reading 스키마 반영, 실패 상태 분기, 9개 fixture 재생 어댑터 구현)
2. 내가 직접 판단한 일: (예: ______________ — 직접 채워주세요)
3. AI 말을 안 들은 일 (없으면 이유): (예: 없음 / 있다면 사유 서술)

---

## 완주 체크리스트 (35개 조건)

### 공개 접근성 — public_access (자동 통과 예상, 배포 후 새 시크릿 창으로 직접 확인 필요)
- [ ] C01 결과물·소스 URL이 계정 생성 없이 열린다
- [ ] C02 로그인 없이 열린다
- [ ] C29 인증 없이 열린다
- [ ] C30 초대 없이 열린다
- [ ] C31 비밀번호 없이 열린다
- [ ] C32 OAuth 없이 열린다
- [ ] C33 CAPTCHA 없이 열린다

> 앱 자체에 로그인/인증 레이어가 없으므로 GitHub Pages로 배포만 하면 통과할 구조입니다. 배포 후 **새 시크릿 창**으로 직접 열어서 확인하세요.

### 실제 라이브 조회 — live_receipt_and_review / live_and_deterministic
- [ ] C03 비개인 공개 원천의 실제 동적 값을 조회할 수 있다 — ✅ Open-Meteo 라이브 연동 확인됨
- [ ] C04 값이 표시된다 — ✅ `reading-value`
- [ ] C05 단위가 표시된다 — ✅ `reading-unit`
- [ ] C06 출처가 표시된다 — ✅ `reading-source`
- [ ] C07 출처 시각이 표시된다 — ✅ `reading-source-time`
- [ ] C08 조회 시각이 표시된다 — ✅ `reading-fetched-at`
- [ ] C09 기준 시간대가 표시된다 — ✅ `reading-timezone` (Asia/Seoul 고정)
- [ ] C10 원자료·저장값·화면값이 일치한다 — ✅ 동일 `reading` 객체를 그대로 저장/표시
- [ ] **C22 서로 다른 실제 날짜 기록 정확히 2건** — ⚠️ 09-02 기록만 있음. **내일(09-03) 실제 조회 필요**
- [ ] **C23 두 기록의 출처URL·출처시각·값·단위가 저장값·화면값과 일치** — C22 충족 후 재확인
- [ ] **C24 두 기록을 날짜순으로 재계산한 변화값이 화면값과 일치** — C22 충족 후 재확인 (비교 로직은 구현·테스트 완료)

### 보안·개인정보 — source_and_runtime_scan
- [ ] C11 비밀키 원문 0건 — ✅ Open-Meteo는 API 키 불필요, 코드에 키 없음
- [ ] C25 개인정보·개인 기록 0건 — ✅ 위치명 외 개인 식별 정보 없음

### 합성 재생 — deterministic_replay (✅ 전부 확인 완료, "합성 재생" 탭에서 재현 가능)
- [ ] C12 느린 응답(TIMEOUT) → 별도 실패 상태 — ✅
- [ ] C13 401/403(AUTH) → 별도 실패 상태 — ✅
- [ ] C14 호출 제한(429) → 별도 실패 상태 — ✅
- [ ] C15 오프라인 → 별도 실패 상태 — ✅
- [ ] C16 형식 변경(SCHEMA-BREAK) → 별도 실패 상태 — ✅
- [ ] C17 실패 뒤 마지막 정상값 안 지워짐 — ✅
- [ ] C18 실패 뒤 정상값에 오래된 값 표시 — ✅
- [ ] C19 재시도 → RECOVER-D2 재생 시 fresh/none, 다음 날짜 행 1건 추가 — ✅
- [ ] C20 같은 날짜 중복 성공 → 기록 1건 — ✅ (NORMAL-D1-A/B 시퀀스로 확인)
- [ ] C21 다음 날짜 성공 → 새 행 생성 — ✅ (NORMAL-D2로 확인)
- [ ] C26 다섯 실패 재생에 합성 시험값만 사용 — ✅ fixtures/ 파일만 사용, 실제 API 호출 없음

### 제출문 — submission_text
- [ ] C27 짧은 확인 4줄 구분 — 위 "재현·통과 확인" 참고해 다듬기
- [ ] C28 AI/직접 판단 3줄 구분 — 직접 채우기 (AI가 대신 쓰면 안 됨)

### 제출 필드 — submission_field
- [ ] C34 결과물 URL 필드에 HTTPS 1개 — 배포 후 채우기
- [ ] C35 소스 URL 필드에 HTTPS 1개 + 40/64자리 commit 해시 포함 — 배포 후 채우기
